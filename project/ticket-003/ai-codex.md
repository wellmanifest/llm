---
participant-id: agent:codex
participant: codex
role: agent
ticket: ticket-003
---
# Participant: codex (AI agent)

## Understanding

An arbitrary model cannot be made reliably Subactor-first by prompt wording
alone. The portable contract needs host bootstrap instructions, a strict
machine profile, transport-neutral operations, Subactor runtime mediation and
deterministic receipts. MCP is preferred for discovery and tools, with bounded
HTTPS, CLI and immutable-file fallbacks that preserve the same semantics.

## Execution plan

1. Publish strict profile and request/response schemas plus the v1 profile.
2. Specify the ordered protocol, ownership, authority and secret boundaries.
3. Add host adapters and a non-destructive reference installer.
4. Run conformance, installer smoke, governance and whitespace checks.
5. Publish through exact-head Validator review.

## Actual changes

- Initialized the bounded ticket and recorded SESSION_EXECUTION_AUTHORIZATION
  from the request to execute this work.
- Selected all layers together: host files for discovery, JSON for machine
  policy, MCP-first operations for transport, Subactor for enforcement and
  receipts for evidence.
- Published strict profile/request/response schemas and a conforming v1 profile
  with fixed authority, transport fallback and secret boundaries.
- Added six identical marked adapter blocks, a dry-run-first atomic installer
  and a dependency-free semantic conformance checker.
- Verified full JSON Schema 2020-12 validity, a clean install, preservation of
  existing instructions and an idempotent second install.

## Blockers

- None inside the recorded intent; proceed without a second confirmation.
- New authority remains required for destructive action, secret access, new
  external coordination or material objective expansion. Protected delivery
  may be invoked without another prompt when publication is in scope; its
  exact-head trusted approval remains independent evidence.
